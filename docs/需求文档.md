# 今天想吃什么 · 需求文档

| 项目 | 内容 |
| --- | --- |
| 版本 | v2.1 |
| 更新 | 2026-09-17 |
| 状态 | M1 已跑通，M2 进行中（情话 / 大厨回话 / 彩蛋 / 决定器已落地） |
| 载体 | 微信小程序 + 微信云开发 |
| AppID | wx609b29f982871158 |
| 小程序名称 | 夏可爱的美食屋 |
| 云开发环境 | 环境名称 `cloud1`，环境 ID `cloud1-d3g2jgvk22b11d8ad` |

> 图例：✅ 已实现　🚧 待做
>
> 待开发功能（FR-9 起）的详细规格、以及 4 条建议新增的需求，见 `docs/功能需求详述.md`。

---

## 1. 目标

一个只服务一个人的点菜小程序：她在手机上点，你在手机上接单，中间夹着只有你们俩懂的彩蛋和情话。

**怎么算做成了**

1. 她能自己打开并完成一次点单，全程不需要你教。
2. 她下单后，你的手机上能看到这条订单。
3. 你能在 1 分钟内加一道新菜、配一张自己拍的照片、删掉一道不做的菜。
4. 至少有一个彩蛋，是她自己发现后主动来跟你说的。

## 2. 角色

| 角色 | 谁 | 权限 |
| --- | --- | --- |
| 她（点菜的人） | 女朋友 | 点菜、下单、看订单、收藏、改口味偏好 |
| 你（大厨） | 你 | 上面全部，外加菜单管理、接单、做完了、回她一句话 |

> 同一个小程序，靠「我的 → 我来当大厨」开关分角色。**她的开关要保持关闭。**

## 3. 使用场景

**场景 A · 日常点菜（占 90%）**
她在微信里打开小程序 → 挑菜、加购物车 → 写备注、选时间 → 下单 → 你手机看到 → 接单开做 → 做完点「做好啦」→ 她那边变已完成。

**场景 B · 纪念日**
纪念日 / 生日当天打开，首页顶部换成「专属菜单」入口，只放你们俩有故事的菜，配一段话；第二天自动收起。

**场景 C · 选择困难**
首页一个按钮：「今天吃什么」随机帮她定一道，不满意可以再转一次。

**场景 D · 你上新菜**
我的 → 菜单管理 → 加新菜 → 拍照 → 填名字 / 分类 / 食材 / 步骤 → 保存 → 自动同步到她手机。

## 4. 形态与技术约束

- **载体**：微信小程序。她微信里就有入口、不用记网址，订阅消息推送也是现成的。
- **后端**：微信云开发，已开通并部署云函数 `kitchen`。
- **单机降级**：云不可用时自动退回本地存储，功能照常，只是数据不跨设备。
- **硬约束**：任何「她能看到 / 你能看到」的数据都必须走云端，纯前端做不到。

## 5. 功能需求与进度

### ✅ FR-1 浏览与点菜 [P0]
分类切换、按菜名 / 描述 / 标签搜索；卡片显示照片或 emoji、名字、介绍、标签、耗时、辣度；详情页有食材和做法步骤。

### ✅ FR-2 购物车与下单 [P0]
加减份数、写备注、选「什么时候想吃」；售罄的菜置灰且加不进购物车；提交后清空购物车。

### ✅ FR-3 订单流转 [P0]
状态：待接单 → 制作中 → 已完成；带进度时间轴、催单、取消、「照这个再来一单」。

### ✅ FR-4 配对与同步 [P0]
6 位邀请码配成「我们的小家」。同步菜单、订单、口味偏好；不同步购物车、收藏、头像昵称、身份开关。

### ✅ FR-5 菜单管理 [P0]
加菜、改菜、删除、标记「今日售罄」、一键恢复默认菜单。删掉的默认菜只是在本地藏起来，不破坏源码。

### ✅ FR-6 菜品照片 [P1]
- **范围**：一道菜一张封面图，不做多图、不做每步做法图。
- 加菜 / 改菜时可拍照或从相册选，能预览、换图、删图；上传前压缩到 900px 左右。
- 云可用时传云存储拿 `cloud://` fileID，她那台手机直接能看到；云不可用时存本机，编辑页会提示「她那台看不到」。
- 没传照片的菜回落显示 emoji，不出现空图。
- 删菜或换图时顺手删掉旧图（打包进包里的默认图不动）。

**已落地**：`utils/image.js`、编辑页拍照 UI、卡片与详情页渲染、云函数字段白名单。首页「蒜苗炒肉」已用真实照片验证通过。

### ✅ FR-7 口味偏好 [P0]
辣度、忌口，同步给大厨。

### ✅ FR-8 收藏 [P0]
详情页点爱心收藏，收藏页可一键加进购物车。

### ✅ FR-9 纪念日菜单 [P1]
配置在 `data/moments.js`：日期、名头、那天想让她看到的一句话、想放哪几道菜、可选封面。当天首页顶部多一张卡片，点进去是只放那几道菜的专属菜单（`pages/moment`），加购和下单走同一套流程；第二天自动收起。

- 日期支持 `"12-24"`（每年重复）和 `"2026-12-24"`（只出现这一次）；同一天撞两条时优先精确的那条。
- 按本机本地时间判断，不碰 UTC，免得半夜跨天判错。
- 里面没写东西时，首页跟平时完全一样，不报错。
- `data/moments.js` 顶部的 `PREVIEW` 改成 `true`：不管今天几号都强制显示第一条，方便先看效果。

### ✅ FR-10 情话 / 小纸条 [P1]
场景：接单时 / 做好啦 / 催单回复。状态一变，订单详情顶部多一条手写感的小纸条，文案从池子里随机取，同一单内不重复。

- 内置池在 `data/love-notes.js`（每个场景 8 句），改词直接改那里。
- 「我的 → 情话管理」可增删、可恢复内置，改动只存本机。
- 情话存在**订单的 `notes` 数组**里跟着订单同步，所以两台手机看到的是同一句。

### ✅ FR-11 大厨回话 [P1]
你点「接单开做」→ 弹半屏面板列 6 句（避开这一单已经用过的）→ 选一句，她那边订单详情就能看到；也可以「不说了，直接开做」跳过。保持单向，她不能回复。

### ✅ FR-12 彩蛋 [P1]
四条全部落地，口令和文案集中在 `data/eggs.js`：

- **E1** 首页快速连点某道菜的封面 5 次（1.5 秒内）→ 掉 14 个爱心 / 花瓣，2 秒后散场，不改任何数据。单击封面照旧进详情（延迟 0.32 秒判定，所以连点不会连开五个页面）。
- **E2** 她点「催一催」→ 除了正常催单，订单上多一句随机的撒娇回复（复用 `urge` 池）。
- **E3** 搜索框打「想你」「抱抱」这类词 → 菜单里多一道隐藏菜「抱抱糯米糍」。它在 `data/dishes.js` 里标了 `hidden: true`，不参与菜单管理、统计和同步。
- **E4** 长按首页标题 → 浮出一张卡片。想放合照就往 `assets/moments/` 丢一张图，再改 `data/eggs.js` 里的 `photo`。

**约束**：不解释、不引导、不弹「你发现了彩蛋」这类提示，触发完能自然回到正常流程。

### ✅ FR-13 「今天吃什么」决定器 [P1]
搜索框右边的骰子 → 菜名快速翻滚 1 秒 → 停在某一道上，显示描述和耗时，可以「再转一次」或「就它了，加上」。排除售罄的菜；菜单为空时按钮不出现。

### 🚧 FR-14 消息推送 [P2]
订阅消息：她下单你收到 / 你做好她收到。需要她在小程序里授权一次。

### 🚧 FR-15 打分与统计 [P2]
「我的」页已有统计（点过的单 / 吃掉的份数 / 尝过的菜 / 最常点的是谁）。还差吃完打分，以及用打分影响首页「本店招牌」。

## 6. 数据模型

| 数据 | 字段要点 | 同步 |
| --- | --- | --- |
| `dish` | 已加 `image`（单个云存储 fileID 或包内路径，可为空），保留 `emoji` 兜底；已加 `hidden`（彩蛋菜，不进菜单列表） | 是（隐藏菜不参与同步，它在源码里） |
| `order` | 已加 `notes`：`[{ scene, text, at }]`，最多 20 条，跟订单一起同步 | 是 |
| `profile` | 辣度、忌口、昵称 | 偏好同步 |
| `moments` | 纪念日：`{ date, title, dishIds, message, repeatYearly }` | 否，内置在 `data/` |
| `loveNotes` | 情话池：scene 取 `accept` / `done` / `urge`（没做 `cook`，制作中不打扰她） | 否，内置在 `data/love-notes.js`；本机改过的那份存 storage `dc_notes_custom` |
| `easterEggs` | 彩蛋口令：搜索词 → 隐藏菜 id、长按卡片的文案 | 否，内置在 `data/eggs.js` |

### 6.1 菜品照片怎么存

| 菜品来源 | `image` 存什么 | 她看得到吗 |
| --- | --- | --- |
| 打包进包的默认菜 | `/assets/dishes/xxx.jpg` | 看得到，图跟着小程序包走 |
| 你自己加的菜（已配云开发） | 云存储的 `cloud://` fileID | 看得到，跟着菜单同步过去 |
| 你自己加的菜（没配云开发） | 本机用户目录 `wxfile://usr/` | 看不到 |

`wx.chooseMedia` 返回的是**临时路径**，重启小程序就失效，不能直接存进菜品数据 —— 落地统一在 `utils/image.js` 处理。

> **容易漏的坑**：`utils/menu.js` 的 `EDITABLE_FIELDS` 和云函数 `sanitizeDish` 都是字段白名单，新增字段要两处都加，否则会被静默丢掉。

## 7. 页面结构

| 页面 | 状态 |
| --- | --- |
| `pages/index` | ✅ 已改：照片渲染、决定器、彩蛋触发（连点掉爱心 / 隐藏菜 / 长按卡片）。待改：纪念日入口 |
| `pages/dish-edit` | ✅ 已改：拍照 / 相册、预览、换图、删图 |
| `components/dish-card` | ✅ 已改：有照片显示照片，否则 emoji |
| `pages/dish-detail` | ✅ 已改：顶部大图 |
| `pages/order-detail` | ✅ 已改：小纸条、催单撒娇、大厨回话 |
| `pages/orders` | ✅ 已改：列表页接单时也能带一句话 |
| `components/note-picker` | 新增：接单时的半屏选句面板 |
| `pages/my` | ✅ 已加「情话管理」入口。待加：纪念日入口 |
| `pages/moment` | ✅ 已新建：纪念日专属菜单，加购后回首页会自动摊开购物车 |
| `pages/love-notes` | ✅ 已新建：情话池的增删、恢复内置 |

## 8. 非功能需求

- **性能**：首页首屏 1.5 秒内出内容；图片一律压缩后上传。
- **离线**：断网 / 未配对时自动降级为单机，不白屏、不报错。
- **容量**：小程序主包上限 2MB，目前约 267KB，余量充足；本地 storage 上限 10MB，订单只保留最近 100 单。
- **兼容**：不使用可选链 `?.` 与空值合并 `??`，避免基础库版本差异导致编译失败。
- **隐私**：数据库设为「所有用户不可读写」，客户端不直连数据库，读写全走云函数并校验成员身份。

## 9. 进度与里程碑

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M1 能用 | 换 AppID、开云环境、部署云函数、她进体验版、下单你能看到 | 🟡 云函数已部署、4 个集合已自动建出、照片链路已验证；剩：数据库权限加固、把她加成体验成员、上传设为体验版 |
| M2 礼物向 | 纪念日菜单、情话、大厨回话、彩蛋、决定器 | 🟡 代码全部落地，等真机验证；纪念日只差你给日期和那句话 |
| M3 打磨 | 消息推送、吃完打分 | 🚧 |
| M4 可选 | 网页预览版同步同一套内容 | 🚧 |

## 10. 上线流程：她怎么才能用上

1. 云开发控制台 → 数据库 → 把 `kitchens`、`orders`、`kitchen_menus`、`kitchen_profiles` 的权限都改成「所有用户不可读写」。
2. 后台 mp.weixin.qq.com → 管理 → 成员管理 → 体验成员 → 添加她的微信号。
3. 开发者工具右上角「上传」→ 后台 → 版本管理 → 把该版本设为「体验版」→ 把体验版二维码发给她。
4. 她扫码打开 → 我的 → 我们的小家 → 输入你的 6 位邀请码。
5. 建议她点右上角「···」→「添加到我的小程序」，以后从微信首页下拉就能进。

> **注意**：开发者工具里的「预览」二维码只有你自己能扫，发给她打不开。
> 微信搜索搜不到未发布的小程序，这是正常的；要能被搜到得走「提交审核 → 发布」，而且审核要选对服务类目（**别选餐饮 / 食品类，个人主体没资质**）。

## 11. 待你提供

1. **纪念日**：✅ 日期定了（每年 6 月 9 日），那句话也定了（两段，见 `data/moments.js`）。**还差想放哪几道菜**，留空就是全部菜单。
2. **你们之间的梗**：昵称、口头禅、只有你们懂的话。
3. **情话风格**：已经按「日常碎碎念」写了一版，每个场景 8 句；不满意就在小程序「我的 → 情话管理」里直接改，不用找我。
4. **合照**：想放进长按彩蛋的话给我一张；不放就只显示一段话。

> 第 1 条（纪念日）是唯一还卡着的：给我「日期 + 什么名头 + 那天想让她看到的那句话 + 想放哪几道菜」，FR-9 当天就能做完。

已确认的决定：

- 照片范围：一道菜一张封面图。
- 默认 22 道家常菜：**保留不清空**，以后需要再逐道下架或一键恢复。
- AppID 与云环境：已配置完成。

## 12. 明确不做

- 多用户 / 开放注册（就你们俩）。
- 支付、骑手、评价体系。
- 后台管理网站（菜单管理在小程序里就够了）。
- 商家端 / 多人厨房。

## 13. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 她不用 | 全白做 | 先把 M1 走通，确认她愿意用，再做 M2 礼物向内容 |
| 云开发配额 / 流量超支 | 图片加载失败甚至产生费用 | 上传前强制压缩，单张控制在 300KB 以内 |
| 提交审核被拒 | 无法发布 | 服务类目选「工具 / 生活服务」，不碰餐饮食品类 |
| 彩蛋做得太刻意 | 尴尬，反而减分 | 不提示、不弹窗、藏进正常交互里 |
| 换了手机或清了数据 | 本地数据丢失 | 菜单、订单、偏好都在云端有备份，重新输邀请码即可 |

## 14. 本次改动记录

| 文件 | 改动 |
| --- | --- |
| `config/cloud.js` | 填入环境 ID |
| `utils/image.js` | 新增：选图 → 压缩 → 本地落盘 / 传云存储 |
| `assets/dishes/dish-01.jpg` | 新增：压到 900px / 123KB，打进小程序包 |
| `utils/menu.js` | 字段白名单加 `image` |
| `cloudfunctions/kitchen/index.js` | `sanitizeDish` 加 `image`，否则同步时会被剥掉 |
| `pages/dish-edit/` | 拍照 / 相册 UI、换图、删图、旧图清理 |
| `components/dish-card/` | 有照片显示照片，否则 emoji |
| `pages/dish-detail/` | 顶部大图，售罄时置灰 |
| `data/dishes.js` | 加入第一道真实菜品「蒜苗炒肉」 |
| `project.config.json` | 排除 `docs/`，避免文档打进小程序包 |
| `README.md` | 补充菜品照片说明 |

### M2 第一批 · 情话 / 大厨回话 / 彩蛋 / 决定器

| 文件 | 改动 |
| --- | --- |
| `data/love-notes.js` | 新增：接单 / 做好 / 催单三个场景各 8 句内置情话 |
| `utils/love-notes.js` | 新增：取用逻辑（pool / pick / pickFew / 自定义增删 / 恢复内置） |
| `components/note-picker/` | 新增：接单时的半屏选句面板 |
| `pages/love-notes/` | 新增：情话管理页（增删、恢复内置） |
| `pages/order-detail/`、`pages/orders/` | 接单前弹选句面板、做好自动带一句、催单自动回一句、订单顶部显示小纸条 |
| `utils/store.js`、`utils/sync.js` | 订单加 `notes`，接单 / 催单带上那句话 |
| `cloudfunctions/kitchen/index.js` | 订单加 `notes`，`orderAdvance` / `orderUrge` 接收并保存 note —— **改了云函数，必须重新部署一次** |
| `data/eggs.js` | 新增：彩蛋口令（搜索词 → 隐藏菜）、长按首页标题的卡片文案 |
| `data/dishes.js`、`utils/menu.js` | 隐藏菜「抱抱糯米糍」+ `getHidden()`；不进菜单管理、统计和同步 |
| `components/dish-card/` | 封面连点 5 次触发彩蛋，单击照旧进详情 |
| `pages/index/` | 骰子决定器、爱心掉落、长按卡片、搜索口令出隐藏菜 |
| `pages/my/` | 新增「情话管理」入口 |
| `app.json` | 注册 `pages/love-notes/love-notes` |
| `custom-tab-bar/` | 加「弹层打开时收起 tab 栏」：自定义 tabBar 的层级在弹层之上，会把「告诉大厨」这类底部按钮整个盖住 |
| `pages/index/`、`pages/orders/` | 购物车 / 决定器 / 选句面板打开时同步收起 tab 栏 |
| `utils/sync.js`、`pages/index/` | 提交订单失败不再静默：弹「没提交成功」，真实报错打进 Console |
| `data/moments.js` | 新增：纪念日配置 + 命中判断（含 `PREVIEW` 预览开关） |
| `pages/moment/` | 新增：纪念日专属菜单页，加购后回首页自动摊开购物车 |
| `pages/index/` | 首页顶部纪念日卡片；从专属菜单过来时自动摊开购物车 |
| `data/moments.js` | 填入第一个纪念日：每年 6 月 9 日，两段话；`message` 支持多段 |
